{
  "name": "ai-skilll-monorepo",
  "version": "1.0.0",
  "description": "Root configuration for Railway Nixpacks",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "cd backend && npm install",
    "start": "cd backend && node index.js"
  }
}
